@using System.Security.Claims
@model MasterOk.Models.ModelDataBase.ProcentSalary

@{
    ViewData["Title"] = "Удаление скидки";
    var role = User.FindFirstValue(ClaimsIdentity.DefaultRoleClaimType);
    switch (role)
    {
        case "admin": Layout = "~/Views/Shared/Admin_Layout.cshtml"; break;
        case "clientmanager": Layout = "~/Views/Shared/ClientManager_Layout.cshtml"; break;
    };
}

<h1>Вы действительно хотите удалить скидку?</h1>
<hr />
<div>
    <dl class="row">
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.TitleProcentSalary)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.TitleProcentSalary)
        </dd>
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.ValueProcentSalary)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.ValueProcentSalary)
        </dd>
    </dl>

    <div class="form-group mb-3">
        <form asp-action="Delete">
            <input type="hidden" asp-for="Id" />
            <input type="submit" value="Удалить" class="btn btn-danger" />
            <a asp-action="Index" role="button" class="btn btn-secondary">Вернуться к списку</a>
        </form>
    </div>
</div>
